/** 
 * Binary search on a sorted array that has been rotated by <x>
 * e.g. 4,5,6,1,2,3
 */ 

/** 
 * 'Pivot' is actually the element next to 'peak'.
 * in [4,5,6,1,2,3] -> 6 is the peak, 1 is the pivot.
 * Alternately, just find the element when a[i+1] < a[i]
 */
